input {
    redis {
            host => '<%= @input_redis_host %>'
            port => <%= @input_redis_port %>
            data_type => 'list'
            key => '<%= @input_redis_key %>'
            codec => 'json'
        }
}
